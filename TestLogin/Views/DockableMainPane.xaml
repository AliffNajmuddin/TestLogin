<UserControl x:Class="TestLogin.Views.DockableMainPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignWidth="360" d:DesignHeight="480">
    <Border Padding="12" Background="#FFF" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   <!-- header -->
                <RowDefinition Height="Auto"/>   <!-- status -->
                <RowDefinition Height="Auto"/>   <!-- login form -->
                <RowDefinition Height="*"/>      <!-- api message -->
                <RowDefinition Height="Auto"/>   <!-- buttons -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center">
                <Image Grid.Column="0"
                       Source="pack://application:,,,/TestLogin;component/Resources/Icons/mimar.png"
                       Width="50"
                       Height="36"
                       VerticalAlignment="Top"
                       Margin="0,0,8,0"
                       ToolTip="Company Logo"/>
                <StackPanel Margin="8,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="AppTitleText" FontWeight="Bold" FontSize="14" Foreground="#222">
                        <Run Language="en-my" Text="Mimar Tech"/>
                    </TextBlock>
                    <TextBlock x:Name="WelcomeTextBlock" Text="Please sign in" FontSize="11" Foreground="#666"/>
                </StackPanel>
            </StackPanel>

            <!-- Status / token expiry -->
            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <TextBlock x:Name="StatusTextBlock" Text="Not authenticated." FontWeight="SemiBold" Foreground="#333"/>
                <TextBlock x:Name="ExpiresTextBlock" Text="" FontSize="11" Foreground="#888" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Inline login form -->
            <Border x:Name="LoginFormBorder" Grid.Row="2" Margin="0,12,0,0" Background="#FFF" Padding="6" CornerRadius="4">
                <StackPanel>
                    <!-- Username with watermark -->
                    <Grid>
                        <TextBox x:Name="UsernameTextBox" Height="28" />
                        <TextBlock x:Name="UsernameWatermark"
                                   Text="Email"
                                   Foreground="#888"
                                   Margin="6,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- Password with watermark (PasswordBox cannot be bound directly) -->
                    <Grid Margin="0,8,0,0">
                        <PasswordBox x:Name="PasswordBox" Height="28" />
                        <TextBlock x:Name="PasswordWatermark"
                                   Text="Password"
                                   Foreground="#888"
                                   Margin="6,0,0,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                        <CheckBox x:Name="RememberMeCheckBox" VerticalAlignment="Center" />
                        <TextBlock Text=" Remember me" VerticalAlignment="Center" Foreground="#444"/>
                        <TextBlock x:Name="LoginErrorText" Foreground="#B00020" Margin="8,0,0,0" Visibility="Collapsed"/>
                    </StackPanel>
                    <ProgressBar x:Name="LoginProgress" Height="6" Margin="0,8,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <!-- Message area -->
            <Border Grid.Row="3" Margin="0,12,0,12" Background="#FBFBFD" BorderBrush="#EEE" BorderThickness="1" Padding="10" CornerRadius="4" Visibility="Collapsed" x:Name="MessageBorder">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="220">
                    <TextBlock x:Name="MessageTextBlock" TextWrapping="Wrap" Foreground="#222"/>
                </ScrollViewer>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="LoginButton" Width="100" Height="28" Content="Sign in" Margin="0,0,8,0"/>
                <Button x:Name="LogoutButton" Width="100" Height="28" Content="Sign out" Margin="0,0,8,0"/>
                
            </StackPanel>
        </Grid>
    </Border>
</UserControl>